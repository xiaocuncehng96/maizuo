第一次修改
删除远程分支：git push origin :分支名


分支操作：
把项目克隆下来 克隆地址后加上 自己的名字

进入到自己的那个文件夹下

创建自己的分支

更改项目后把在远程添加自己的分支：git push --set-upstream origin 分支名

然后git push 
(每次修改后进行本地提交然后直接git push)

这时候主分支还是没有修改后的内容，主分支需要git fetch --all
git merge 分支名